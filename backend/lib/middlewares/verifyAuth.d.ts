import { NextFunction } from "express";
import { ReqType, ResType } from "../types";
declare const _default: (req: ReqType, res: ResType, next: NextFunction) => Promise<ResType>;
export default _default;
