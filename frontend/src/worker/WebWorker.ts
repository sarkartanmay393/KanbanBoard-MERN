export default onmessage = (event: MessageEvent<string>) => {
  // Perform some calculations or tasks
  // const result = processData(data);
  // postMessage(result);
  // console.log(event.data);
};
